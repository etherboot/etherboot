/* When linking with an uncompressed image, these symbols are not
 * defined so we provide them here.
 */

decompressor_uncompressed = 0 ;
decompressor__start = 0 ;

INCLUDE arch/i386/core/start16z.lds
